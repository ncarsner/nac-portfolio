[project]
name = "nac-portfolio"
version = "0.1.0"
description = "Personal portfolio: a catalogue of apps, tools, published packages and writing."
requires-python = ">=3.12"
dependencies = [
    "pelican>=4.9",
    "markdown>=3.6",
]

[dependency-groups]
dev = ["pytest>=8"]
